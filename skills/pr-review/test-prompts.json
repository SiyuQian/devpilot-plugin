[
  {
    "id": 1,
    "scenario": "happy-path / typical PR review",
    "prompt": "review PR #139 in this repo",
    "expected": "Eligibility gate runs → graph preflight → 5 parallel subagents (A–E) + F if dependencies added → filter at confidence 70 → single combined POST with inline comments anchored to lines + body containing Verdict / TL;DR / Strengths / Unknown-Unknowns Sweep / inline counts. Body uses full-SHA blob links. Event derives from highest severity."
  },
  {
    "id": 2,
    "scenario": "eligibility-gate short-circuit",
    "prompt": "review this dependabot PR: https://github.com/example/repo/pull/42 (bumps lodash from 4.17.20 to 4.17.21)",
    "expected": "Stops at eligibility gate with explicit reason (automation-only / generated-only). No fanout dispatched, no review posted. Tells user why."
  },
  {
    "id": 3,
    "scenario": "local diff, no PR URL, no posting",
    "prompt": "review my local branch diff against main before I push — don't post anything, just show me findings",
    "expected": "Loads via git diff main...HEAD (not gh). Runs fanout. Skips graph preflight gracefully if no head-SHA on remote. Renders body + inline comments locally with `path:line` prefixes (per posting.md → 'Skip posting'). Does NOT call gh api or pr-review-project.sh."
  },
  {
    "id": 4,
    "scenario": "configured GitHub Project lifecycle",
    "prompt": "review https://github.com/example/repo/pull/73; this repo has devpilot.reviewProject configured",
    "expected": "Eligibility gate runs first. If it proceeds, the verified pr-review-project.sh wrapper moves the PR to `Being reviewed` before fanout. The skill posts one combined review, then and only then moves the item to `Reviewed`."
  },
  {
    "id": 5,
    "scenario": "project sync unavailable",
    "prompt": "review https://github.com/example/repo/pull/74; track it on our configured GitHub Project",
    "expected": "If the wrapper reports missing `project` scope or another board error, the skill reports the exact short failure and continues the full review and POST. It does not claim the board was updated and does not weaken or skip the code review."
  },
  {
    "id": 6,
    "scenario": "board setup only",
    "prompt": "configure PR review tracking for this repo on https://github.com/orgs/example/projects/9",
    "expected": "Resolves the verified wrapper, runs setup against the explicit existing Project, and saves the URL in repository-local git config. If authorization is missing, asks the user to run `gh auth refresh -s project`. Does not create a Project, run a PR eligibility gate, or start review fanout."
  }
]
